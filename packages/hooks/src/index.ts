/**
 * @tahoe-ui/hooks
 *
 * Reusable React hooks for UI interactions.
 *
 * @example
 * ```tsx
 * import { useSwipe, useFocusTrap, useBreakpoint } from '@tahoe-ui/hooks';
 * // or import specific modules
 * import { useLongPress } from '@tahoe-ui/hooks/gestures';
 * import { useClickOutside } from '@tahoe-ui/hooks/dom';
 * ```
 */

// Gesture primitives
export * from './gestures';

// DOM interaction hooks
export * from './dom';

// Utility hooks
export * from './utility';

// Interaction hooks
export * from './interaction';
